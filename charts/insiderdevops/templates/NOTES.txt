✅ {{ include "insiderdevops.fullname" . }} deployed to namespace "{{ .Release.Namespace }}".

Release : {{ .Release.Name }}
Image   : {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}
Replicas: {{ .Values.replicaCount }}

1. Watch the rollout:

   kubectl rollout status deployment/{{ include "insiderdevops.fullname" . }} -n {{ .Release.Namespace }}

{{- if .Values.ingress.enabled }}

2. Service URL (via nginx Ingress):

   http{{ if .Values.ingress.tls }}s{{ end }}://{{ .Values.ingress.host }}{{ .Values.ingress.path }}

   On Track B (minikube, Windows) make the host resolve, then open the tunnel:

     - Add this line to C:\Windows\System32\drivers\etc\hosts (admin):
         127.0.0.1 {{ .Values.ingress.host }}
     - In a separate terminal, run:  minikube tunnel
     - Then browse:  http://{{ .Values.ingress.host }}/ping
{{- else }}

2. Ingress is disabled. Reach the ClusterIP Service with a port-forward:

   kubectl port-forward -n {{ .Release.Namespace }} svc/{{ include "insiderdevops.fullname" . }} 8080:{{ .Values.service.port }}
   # then: curl http://127.0.0.1:8080/ping
{{- end }}

3. Quick checks:

   kubectl get pods,svc,ingress -n {{ .Release.Namespace }} -l app.kubernetes.io/instance={{ .Release.Name }}
   helm history {{ .Release.Name }} -n {{ .Release.Namespace }}
